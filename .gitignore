# build output — target/ is a crime scene, don't commit it
target/
*.class
*.jar
dependency-reduced-pom.xml

# your db password lives in here — committing it = free db access for strangers :skull:
# (copy config/db.example.properties -> config/db.properties)
config/db.properties

# IDEs (please do not touch this section unless you wanna push the entire IDE related settings and everything to main :sob:)
.idea/
*.iml
.vscode/
.settings/
.classpath
.project
nbproject/
nbactions.xml

# OS junk nobody asked for
.DS_Store
Thumbs.db
