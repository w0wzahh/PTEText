# PTEText database configuration.
# Copy this file to config/db.properties and adjust for the fuckass gay machine.
# The defaults match a standard XAMPP install (root user, empty password or change it however you want idrk).

db.host=localhost
db.port=3306
db.user=root
db.password=

# Names of the three databases (see sql/01_create_databases.sql we can change it later on this is just for something for now)
db.name.users=ptetext_users
db.name.chat=ptetext_chat
db.name.system=ptetext_system
